server:
  port: 8080
  context-path: /

user:
  username: 开发环境
  age: 22
  description: "姓名：${user.username}, 年龄：${user.age}"

spring:
  datasource:
      username: root
      password: 123456
      url: jdbc:mysql:///springboot
      driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

